// CarotidCheck — default widget smoke test.
// Domain tests live under test/core/ (e.g. analysis_model_test.dart).

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('smoke: test harness runs', () {
    expect(2 + 2, 4);
  });
}
